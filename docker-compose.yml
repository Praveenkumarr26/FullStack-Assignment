version: '3.8'

services:
  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: videoadmin
      MONGO_INITDB_ROOT_PASSWORD: videosecure

volumes:
  mongo_data:
